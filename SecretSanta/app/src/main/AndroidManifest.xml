<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.androidapp.secretsanta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/secret_gift"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/secret_gift"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ManageEventActivity"></activity>
        <activity android:name=".ParticipantListActivity" />
        <activity android:name=".ManageParticipantActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>